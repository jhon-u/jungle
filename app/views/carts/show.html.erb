<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if cart_subtotal_cents.zero? %>
    <%= render :partial => "empty" %>
  <% else %>
    <%= render :partial => "cart" %>
  <% end %>

</section>
