<li class="nav-item dropdown">
  <a
    class="nav-link dropdown-toggle"
    href="#"
    id="navbarDropdownUser"
    role="button"
    data-bs-toggle="dropdown"
    aria-expanded="false"
  >
    <% if current_user %>
      <span class="">
        <i class="fa-solid fa-user"></i><span> <%= current_user.first_name %></span> 
      </span>
    <% else %>
      <span class="text-danger">
        Guest
      </span>
    <% end %>
    <span class="caret"></span>
  </a>
  <ul class="dropdown-menu dropdown-menu-dark">
    <% if current_user %>
      <li><%= link_to 'Logout', :logout, class: "nav-link" %></li>
    <% else %>
      <li><%= link_to 'Login', :login, class: "nav-link" %></li>
      <li><%= link_to 'Signup', :signup, class: "nav-link" %></li>
    <% end %>
  </ul>
</li>